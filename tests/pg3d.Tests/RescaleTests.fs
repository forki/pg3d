module pg3d.Tests.Rescale

open pg3d

open System
open NUnit.Framework

//open FsUnit.Xunit
//open FsCheck
//open FsCheck.Xunit
(*
[<Property>]//(QuietOnSuccess = true, StartSize = Int32.MinValue, EndSize = Int32.MaxValue)>]
let ``Rescale of inputMin produces outputMin`` (inputMin:float) (inputMax:float) (outputMin:float) (outputMax:float) =
    (Rescale.FromTo inputMin inputMax outputMin outputMax inputMin) = outputMin

[<Property>]
let ``Rescale of inputMax produces outputMax`` (inputMin:float) (inputMax:float) (outputMin:float) (outputMax:float) =
    (Rescale.FromTo inputMin inputMax outputMin outputMax inputMax) = outputMax
*)

